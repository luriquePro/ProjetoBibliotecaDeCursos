import { describe, test, jest, afterEach, beforeEach, expect } from "@jest/globals";

describe("#UserService Suite", () => {
	describe("#Register", () => {
		test("Test", () => {
			expect(1).toBe(1);
		});
	});
});
